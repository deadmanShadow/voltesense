// PostCSS config — Tailwind v3 requires these two plugins in this order.
// MUST follow frontend.md §3 (Tailwind setup).
export default {
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
