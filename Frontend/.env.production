# Production env. In real deployments, set VITE_API_BASE_URL at build time
# (e.g. https://voltsense.local). Left empty here so the production build
# is served from the same origin as the backend (recommended for local-first).
VITE_API_BASE_URL=
